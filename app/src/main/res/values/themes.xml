<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème principal DayNight (suit le toggle clair/sombre) -->
    <style name="Theme.DriveMax" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorWhite</item>
    </style>

    <!-- Thème Splash -->
    <style name="Theme.DriveMax.Splash" parent="Theme.DriveMax">
        <item name="android:windowBackground">@drawable/gradient_header</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
